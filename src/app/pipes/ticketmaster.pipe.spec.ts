import { TicketmasterPipe } from './ticketmaster.pipe';

describe('TicketmasterPipe', () => {
  it('create an instance', () => {
    const pipe = new TicketmasterPipe();
    expect(pipe).toBeTruthy();
  });
});
